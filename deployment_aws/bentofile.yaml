service: "service:IndustrialDefectService"

labels:
  project: industrial_defect_detector

include:
  - "*.py"

python: 
  packages: 
    - tensorflow 
    - numpy 
    - opencv-python-headless 
    - pillow 
    - bentoml
    - aws-lambda-powertools

